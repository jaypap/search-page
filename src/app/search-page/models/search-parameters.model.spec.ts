import { SearchParameters.Model } from './search-parameters.model';

describe('SearchParameters.Model', () => {
  it('should create an instance', () => {
    expect(new SearchParameters.Model()).toBeTruthy();
  });
});
