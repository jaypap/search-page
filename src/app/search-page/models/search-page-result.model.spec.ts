import { SearchPageResult.Model } from './search-page-result.model';

describe('SearchPageResult.Model', () => {
  it('should create an instance', () => {
    expect(new SearchPageResult.Model()).toBeTruthy();
  });
});
